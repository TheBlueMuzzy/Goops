---
phase: 24-ui-documentation
plan: 01
type: execute
---

<objective>
Update all user-facing text to use official terminology from GLOSSARY.md.

Purpose: Complete the naming standardization milestone by ensuring players see consistent terms (goop, XP, rank, upgrades) instead of legacy terms (piece, points, level, power ups).
Output: All UI text aligned with official glossary, v1.4 milestone complete.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/GLOSSARY.md
@.planning/phases/23-code-rename/23-07-SUMMARY.md

**Prior Phase:** Phase 23 (Code Rename) completed all variable/type renames. This phase addresses user-facing strings.

**Key Reference:** GLOSSARY.md Terminology Migration Table (lines 291-350)

**Files requiring updates:**
- `components/HowToPlay.tsx` — Tutorial text (4 strings)
- `components/Upgrades.tsx` — Upgrade menu placeholders (3 strings)
- `constants.ts` — Upgrade descriptions (3 strings)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update HowToPlay.tsx tutorial text</name>
  <files>components/HowToPlay.tsx</files>
  <action>
Update the following user-facing strings per GLOSSARY.md:

1. Line ~56: "Swap Hold Piece" → "Swap Held Goop"
2. Line ~105: "Bigger Groups = More Points" → "Bigger Groups = More XP"
3. Line ~106: "Higher Elevation = More Points" → "Higher Elevation = More XP"
4. Line ~130: "Power Points" — Review if this is intentional thematic name or needs update

Note: Read the file first to verify exact line numbers before editing.
  </action>
  <verify>grep -n "Piece\|Points" components/HowToPlay.tsx returns no matches except "Power" if kept as thematic name</verify>
  <done>All tutorial text uses official terminology (goop, XP)</done>
</task>

<task type="auto">
  <name>Task 2: Update Upgrades.tsx menu text</name>
  <files>components/Upgrades.tsx</files>
  <action>
Update the following user-facing strings per GLOSSARY.md:

1. Line ~27: "POWER UPS" → "UPGRADES"
2. Line ~38: "Locked Power Up" → "Locked Upgrade"
3. Line ~39: "Requires Level {i * 5}" → "Requires Rank {i * 5}"

These are placeholder strings in the upgrade menu grid.
  </action>
  <verify>grep -n "Level\|Power Up" components/Upgrades.tsx returns no matches</verify>
  <done>Upgrade menu uses official terminology (upgrades, rank)</done>
</task>

<task type="auto">
  <name>Task 3: Update constants.ts upgrade descriptions</name>
  <files>constants.ts</files>
  <action>
Update the following upgrade description strings in the UPGRADES array:

1. Line ~140 (RESPEC_UPGRADE desc): "Can add or remove points via respec" → "Can add or remove scraps via respec"
2. Line ~238 (HOLD_PREVIEW desc): "Shows the goop currently in holding" → "Shows the goop currently held"
3. Line ~251 (CRACK_MATCHER desc): Fix grammar "upcoming goop match" → "upcoming goop matches"

Note: These are in the `desc` property of upgrade objects. Read file to confirm exact locations.
  </action>
  <verify>npm run test:run passes, grep for old phrasings returns no matches</verify>
  <done>All upgrade descriptions use official terminology and correct grammar</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `npm run test:run` passes (151 tests)
- [ ] `npm run build` succeeds without errors
- [ ] grep -r "Power Up\|Power Ups" src/ returns no matches
- [ ] grep -r "Requires Level" src/ returns no matches
- [ ] grep -r "Hold Piece" src/ returns no matches
- [ ] Manual inspection: HowToPlay shows "Goop" and "XP" terminology
</verification>

<success_criteria>

- All 10 user-facing strings updated
- All verification checks pass
- No TypeScript errors
- Phase 24 and v1.4 milestone complete
</success_criteria>

<output>
After completion, create `.planning/phases/24-ui-documentation/24-01-SUMMARY.md`:

# Phase 24 Plan 01: UI Text Updates Summary

**Updated all user-facing text to use official Goops terminology**

## Accomplishments

- Updated HowToPlay.tsx tutorial text (piece → goop, Points → XP)
- Updated Upgrades.tsx menu text (Power Ups → Upgrades, Level → Rank)
- Updated constants.ts upgrade descriptions (terminology + grammar)

## Files Modified

- `components/HowToPlay.tsx` - Tutorial terminology
- `components/Upgrades.tsx` - Menu placeholder text
- `constants.ts` - Upgrade descriptions

## Decisions Made

[Document any decisions about "Power Points" or other ambiguous terms]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

v1.4 Naming Standardization milestone complete. Ready for next milestone.
</output>
